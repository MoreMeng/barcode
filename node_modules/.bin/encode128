#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../code-128-encoder/encode128" "$@"
  ret=$?
else 
  node  "$basedir/../code-128-encoder/encode128" "$@"
  ret=$?
fi
exit $ret
